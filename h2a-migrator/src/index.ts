// Not really how we would typically do this, but it's not that serious
const HUGO_SITE_DIR = __dirname + "../../example_hugo_site";
const ASTRO_SITE_DIR = __dirname + "../../example_astro_site";

function migrateContent() {
  console.log("Migrating content from Hugo to Astro...");
}

migrateContent();
